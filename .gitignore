/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts

# Local DB (legacy SQLite artifacts + backups — app now uses Postgres)
/data/*.db
/data/*.db-*
/data/*.bak*

# Env
.env
.env*.local

# Personal Claude settings (shared config in settings.json is committed)
.claude/settings.local.json

.DS_Store

# Generated TTS audio — served on-demand via /api/tts (no committed files needed)
public/audio/tts/
public/audio/ex/
